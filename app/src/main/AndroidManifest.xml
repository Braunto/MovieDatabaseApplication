<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.usj.session3.exercise">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".GenreParticipation"></activity>
        <activity android:name=".GenresView" />
        <activity android:name=".ActorParticipation" />
        <activity android:name=".Contact" />
        <activity android:name=".AddMovie" />
        <activity android:name=".EditMovie" />
        <activity android:name=".ActorsView" />
        <activity android:name=".ViewMovie" />
        <activity android:name=".MovieList" />
        <activity
            android:name=".ViewMov"
            android:label="@string/title_activity_view_mov"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>